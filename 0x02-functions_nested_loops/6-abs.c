#include "main.h"

/**
*_abs - Computing the absolute value of an integer
*@n: the integer for computing.
*
*Return: The absolute value of the integer.
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
